{
  "name": "pruebas-a-errores-backend",
  "version": "1.0.0",
  "description": "Backend SAP CAP + Express para gestión de errores",
  "type": "module",
  "scripts": {
    "start": "cds-serve",
    "start:server": "node server.js",
    "build": "cds build --clean",
    "lint": "eslint --config ./eslint.config \"srv/**/*.js\" \"server.js\"",
    "test": "npm run lint"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@sap/cds": "^7.9.4",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^7.6.3"
  },
  "devDependencies": {
    "eslint": "^8.52.0"
  }
}
